# Allow npm to ignore optional peer dependency conflicts.
# chromadb declares @google/generative-ai as a peerOptional dep for its
# built-in embedding helper — we don't use that feature (we embed ourselves),
# so this conflict is safe to bypass.
legacy-peer-deps=true
